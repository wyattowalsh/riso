# Claude Code — Riso Maintainer Repo

**SSOT**: Read [AGENTS.md](./AGENTS.md) for all maintainer instructions.

This file is a pointer only. Do not duplicate command tables or boundaries here.

- **Quality**: `make quality` (repo root)
- **Python**: always `uv run`
- **Never edit**: `samples/*/render/` — regenerate with `./scripts/render-samples.sh`
- **Skills**: `.claude/skills/` and `.agents/skills/` — invoke by description
