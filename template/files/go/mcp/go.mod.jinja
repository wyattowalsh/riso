{%- if mcp_module == 'enabled' and 'go' in mcp_languages -%}
module {{ project_slug }}/mcp

go {{ go_version | default('1.24') }}

require (
	github.com/modelcontextprotocol/go-sdk v1.0.0
)
{%- endif %}
