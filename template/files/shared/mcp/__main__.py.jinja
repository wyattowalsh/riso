{%- if mcp_module | lower != "enabled" %}
"""MCP module disabled."""
{%- else %}
"""Make the MCP server module executable.

This allows running the server as a module:
    uv run python -m shared.mcp
"""

from shared.mcp.server import main

if __name__ == "__main__":
    main()
{%- endif %}
