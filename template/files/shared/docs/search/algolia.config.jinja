"""
Algolia DocSearch configuration for documentation.
Generated by Riso template (feature: 018-docs-sites-overhaul)

Environment Variables Required:
- ALGOLIA_APP_ID: Your Algolia application ID
- ALGOLIA_API_KEY: Your Algolia search-only API key (public)
- ALGOLIA_INDEX_NAME: Your index name (default: {{ project_slug }})

Apply for free Algolia DocSearch: https://docsearch.algolia.com/apply/
"""

import os

ALGOLIA_CONFIG = {
    "app_id": os.getenv("ALGOLIA_APP_ID", ""),
    "api_key": os.getenv("ALGOLIA_API_KEY", ""),
    "index_name": os.getenv("ALGOLIA_INDEX_NAME", "{{ project_slug }}"),
    "search_parameters": {
        "hitsPerPage": 20,
        "attributesToRetrieve": ["hierarchy", "content", "url"],
    },
}

# Validation
if not ALGOLIA_CONFIG["app_id"] or not ALGOLIA_CONFIG["api_key"]:
    import warnings
    warnings.warn(
        "Algolia credentials not configured. "
        "Set ALGOLIA_APP_ID and ALGOLIA_API_KEY environment variables."
    )
