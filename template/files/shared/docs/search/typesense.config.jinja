"""
Typesense search configuration for documentation.
Generated by Riso template (feature: 018-docs-sites-overhaul)

Environment Variables Required:
- TYPESENSE_API_KEY: Your Typesense API key
- TYPESENSE_HOST: Typesense server host (default: localhost:8108)
- TYPESENSE_PROTOCOL: http or https (default: http)

Typesense Cloud: https://cloud.typesense.org/ (~$22/month)
Self-hosted: https://typesense.org/docs/guide/install-typesense.html
"""

import os

TYPESENSE_CONFIG = {
    "api_key": os.getenv("TYPESENSE_API_KEY", ""),
    "host": os.getenv("TYPESENSE_HOST", "localhost:8108"),
    "protocol": os.getenv("TYPESENSE_PROTOCOL", "http"),
    "collection_name": "{{ project_slug }}_docs",
    "search_parameters": {
        "query_by": "title,content",
        "num_typos": 2,
        "per_page": 20,
    },
}

# Validation
if not TYPESENSE_CONFIG["api_key"]:
    import warnings
    warnings.warn(
        "Typesense API key not configured. "
        "Set TYPESENSE_API_KEY environment variable."
    )
