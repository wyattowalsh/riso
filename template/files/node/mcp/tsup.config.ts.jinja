{%- if mcp_module == 'enabled' and 'typescript' in mcp_languages -%}
import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm"],
  dts: true,
  sourcemap: true,
  clean: true,
  target: "node20",
  outDir: "dist",
  shims: false,
  splitting: false,
});
{%- endif %}
