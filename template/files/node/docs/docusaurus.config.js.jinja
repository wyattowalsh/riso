{% if docs_site != 'docusaurus' %}
// Docusaurus configuration is not required because docusaurus was not enabled.
// This placeholder keeps the repository structure stable.
module.exports = {};
{% else %}
// @ts-check
// Generated by Riso template (feature: 018-docs-sites-overhaul)

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: '{{ project_name }}',
  tagline: 'Documentation for {{ project_name }}',
  favicon: 'img/favicon.ico',

  url: 'https://{{ project_slug }}.github.io',
  baseUrl: '/',

  organizationName: '{{ project_slug }}',
  projectName: '{{ project_slug }}',

  onBrokenLinks: 'throw',
  onBrokenMarkdownLinks: 'warn',

  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          sidebarPath: './sidebars.js',
        },
        blog: false,
        theme: {
          customCss: './src/css/custom.css',
        },
      }),
    ],
  ],

  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      navbar: {
        title: '{{ project_name }}',
        items: [
          {
            type: 'docSidebar',
            sidebarId: 'tutorialSidebar',
            position: 'left',
            label: 'Documentation',
          },
        ],
      },
      footer: {
        style: 'dark',
        copyright: `Copyright © ${new Date().getFullYear()} {{ project_name }}. Built with Docusaurus.`,
      },
      {% if docs_theme_mode == 'dark' %}
      colorMode: {
        defaultMode: 'dark',
        disableSwitch: false,
        respectPrefersColorScheme: false,
      },
      {% elif docs_theme_mode == 'light' %}
      colorMode: {
        defaultMode: 'light',
        disableSwitch: false,
        respectPrefersColorScheme: false,
      },
      {% else %}
      colorMode: {
        defaultMode: 'light',
        disableSwitch: false,
        respectPrefersColorScheme: true,
      },
      {% endif %}
      {% if docs_search_provider == 'algolia' %}
      algolia: {
        appId: process.env.ALGOLIA_APP_ID || '',
        apiKey: process.env.ALGOLIA_API_KEY || '',
        indexName: process.env.ALGOLIA_INDEX_NAME || '{{ project_slug }}',
        contextualSearch: true,
      },
      {% endif %}
    }),

  {% if docs_interactive_features == 'enabled' %}
  themes: ['@docusaurus/theme-mermaid'],
  markdown: {
    mermaid: true,
  },
  {% endif %}

  plugins: [],
};

module.exports = config;
{% endif %}
