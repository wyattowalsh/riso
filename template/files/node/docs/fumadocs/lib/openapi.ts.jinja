{%- if docs_module == 'enabled' and docs_framework == 'fumadocs' and fumadocs_openapi == 'enabled' and api_module == 'enabled' and api_languages -%}
import { createOpenAPI } from 'fumadocs-openapi/server';
import path from 'node:path';

export const openapi = createOpenAPI({
  input: [path.resolve('./openapi/openapi.yaml')],
});
{%- endif -%}