{%- if docs_module == 'enabled' and docs_framework == 'fumadocs' and api_module == 'enabled' and 'node' in api_languages -%}
---
title: Node.js API
description: Fastify-based REST API reference
---

# Node.js API

{{ project_name }} includes a Fastify-based REST API for high-performance Node.js services.

## Starting the Server

```bash
# Development mode
pnpm dev

# Production build
pnpm build
pnpm start
```

## Configuration

Configure the API via environment variables:

```bash
# .env
RISO_API_NODE_HOST=0.0.0.0
RISO_API_NODE_PORT=3000
NODE_ENV=development
```

## Endpoints

### Health Check

```http
GET /health
```

Returns the health status of the API (container/orchestrator probe).

**Response:**

```json
{
  "status": "healthy",
  "service": "api-node"
}
```

### Root

```http
GET /
```

Returns basic API information.

## Fastify Plugins

The API uses the following Fastify plugins:

- `@fastify/cors` - CORS support
- `@fastify/helmet` - Security headers
- `@fastify/rate-limit` - Rate limiting
- `@fastify/swagger` - OpenAPI documentation

## Type Safety

All routes are fully typed with TypeScript:

```typescript
import { FastifyPluginAsync } from 'fastify';

const routes: FastifyPluginAsync = async (fastify) => {
  fastify.get<{
    Reply: { status: string; service: string };
  }>('/health', async () => {
    return { status: 'healthy', service: 'api-node' };
  });
};

export default routes;
```

{%- if fumadocs_openapi == 'enabled' %}

## Interactive API Reference

<Callout type="info">
  See the [API Reference](/docs/api) for the full interactive OpenAPI documentation.
</Callout>
{%- endif %}
{%- endif -%}
