{%- if docs_module == 'enabled' and docs_framework == 'fumadocs' -%}
---
title: Modules
description: Overview of {{ project_name }} modules
---

# Modules

{{ project_name }} is organized into several optional modules that can be enabled based on your needs.

## Available Modules

{%- if cli_module == 'enabled' %}

### CLI Module

The command-line interface for {{ project_name }}.

- [CLI Reference](/docs/modules/cli)
{%- endif %}

{%- if api_module == 'enabled' and 'python' in api_languages %}

### Python API

RESTful API built with FastAPI.

- [Python API Reference](/docs/modules/api-python)
{%- endif %}

{%- if api_module == 'enabled' and 'node' in api_languages %}

### Node.js API

API services built with Fastify.

- [Node.js API Reference](/docs/modules/api-node)
{%- endif %}

{%- if (graphql_api_module == 'enabled' or 'graphql' in api_features) %}

### GraphQL API

GraphQL API built with Strawberry.

- [GraphQL Reference](/docs/modules/graphql)
{%- endif %}

{%- if (websocket_module == 'enabled' or 'websocket' in api_features) %}

### WebSockets

Real-time communication module.

- [WebSocket Reference](/docs/modules/websockets)
{%- endif %}

{%- if mcp_module == 'enabled' %}

### MCP Integration

Model Context Protocol tools and resources.

- [MCP Reference](/docs/modules/mcp)
{%- endif %}

{%- if ai_tools_module == 'enabled' %}

### AI Coding Tools

MCP harness and multi-agent configuration.

- [AI Tools](/docs/ai-tools)
{%- endif %}

{%- if cli_module == 'disabled' and (api_module != 'enabled' or not api_languages) and mcp_module == 'disabled' %}

No optional modules are currently enabled. You can enable modules by regenerating the project with different options.
{%- endif %}
{%- endif -%}
