{% if saas_infra_module == "enabled" and saas_billing_module == "enabled" %}
/**
 * Unified Billing Service (Stripe / Paddle / LemonSqueezy)
 */

{% if saas_billing_provider == "stripe" %}
import * as stripe from './stripe/client';
import { processWebhook as processStripeWebhook } from './stripe/webhooks';
{% elif saas_billing_provider == "paddle" %}
import * as paddle from './paddle/client';
import { processWebhook as processPaddleWebhook } from './paddle/webhooks';
{% elif saas_billing_provider == "lemonsqueezy" %}
import * as lemonsqueezy from './lemonsqueezy/client';
import { processWebhook as processLemonSqueezyWebhook } from './lemonsqueezy/webhooks';
{% endif %}

export async function createCustomer(params: {
  email: string;
  name?: string;
  metadata?: Record<string, string>;
}) {
  {% if saas_billing_provider == "stripe" %}
  return await stripe.upsertCustomer(params);
  {% elif saas_billing_provider == "paddle" %}
  return await paddle.createCustomer({
    email: params.email,
    name: params.name,
    customData: params.metadata,
  });
  {% elif saas_billing_provider == "lemonsqueezy" %}
  return await lemonsqueezy.createCustomer(params);
  {% endif %}
}

export async function startSubscription(params: {
  customerId?: string;
  customerEmail?: string;
  priceId: string;
  successUrl: string;
  cancelUrl: string;
  trialDays?: number;
  userId?: string;
  metadata?: Record<string, string>;
}) {
  {% if saas_billing_provider == "stripe" %}
  return await stripe.createCheckoutSession(params);
  {% elif saas_billing_provider == "paddle" %}
  return await paddle.createCheckout({
    priceId: params.priceId,
    customerId: params.customerId,
    customerEmail: params.customerEmail,
    successUrl: params.successUrl,
  });
  {% elif saas_billing_provider == "lemonsqueezy" %}
  return await lemonsqueezy.createCheckout({
    priceId: params.priceId,
    customerId: params.customerId,
    customerEmail: params.customerEmail,
    successUrl: params.successUrl,
    cancelUrl: params.cancelUrl,
    userId: params.userId,
    metadata: params.metadata,
  });
  {% endif %}
}

export async function cancelSubscription(subscriptionId: string, immediately = false) {
  {% if saas_billing_provider == "stripe" %}
  return await stripe.cancelSubscription(subscriptionId, immediately);
  {% elif saas_billing_provider == "paddle" %}
  return await paddle.cancelSubscription(subscriptionId, immediately);
  {% elif saas_billing_provider == "lemonsqueezy" %}
  return await lemonsqueezy.cancelSubscription(subscriptionId, immediately);
  {% endif %}
}

export async function updateSubscription(params: {
  subscriptionId: string;
  priceId?: string;
  quantity?: number;
}) {
  {% if saas_billing_provider == "stripe" %}
  return await stripe.updateSubscription(params);
  {% elif saas_billing_provider == "paddle" %}
  return await paddle.updateSubscription(params);
  {% elif saas_billing_provider == "lemonsqueezy" %}
  return await lemonsqueezy.updateSubscription(params);
  {% endif %}
}

{% if saas_billing_provider == "stripe" %}
export async function reportUsage(params: {
  subscriptionItemId: string;
  quantity: number;
  timestamp?: number;
}) {
  return await stripe.reportUsage(params);
}

export async function getUsageRecords(subscriptionItemId: string) {
  return await stripe.getUsageRecords(subscriptionItemId);
}
{% endif %}

export async function createPortalSession(params: {
  customerId: string;
  returnUrl: string;
}) {
  {% if saas_billing_provider == "stripe" %}
  return await stripe.createPortalSession(params);
  {% elif saas_billing_provider == "paddle" %}
  const subscription = await paddle.getSubscription(params.customerId);
  return { url: subscription.managementUrls?.updatePaymentMethod };
  {% elif saas_billing_provider == "lemonsqueezy" %}
  return await lemonsqueezy.createPortalSession(params);
  {% endif %}
}

export async function processWebhook(
  payload: string,
  signature: string,
  timestamp?: string
) {
  {% if saas_billing_provider == "stripe" %}
  return await processStripeWebhook(payload, signature);
  {% elif saas_billing_provider == "paddle" %}
  return await processPaddleWebhook(payload, signature, timestamp ?? '');
  {% elif saas_billing_provider == "lemonsqueezy" %}
  return await processLemonSqueezyWebhook(payload, signature);
  {% endif %}
}

{% if saas_billing_provider == "stripe" %}
export type {
  StripeCustomer,
  StripeSubscription,
  StripeInvoice,
  StripePrice,
} from './stripe/client';
{% elif saas_billing_provider == "paddle" %}
export type { Paddle } from './paddle/client';
{% elif saas_billing_provider == "lemonsqueezy" %}
export type { LemonSqueezyCheckout } from './lemonsqueezy/client';
{% endif %}
{% endif %}
