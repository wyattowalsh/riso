{% if saas_infra_module == "enabled" %}
/**
 * Validate required environment variables at the package root.
 * Run via: pnpm run validate:env
 */
import { env } from '../config/env';

console.log(`Environment valid (NODE_ENV=${env.NODE_ENV})`);
{% endif %}
