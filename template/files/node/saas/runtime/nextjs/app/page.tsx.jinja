{% if saas_infra_module == "enabled" and saas_runtime == "nextjs-16" %}
import Link from 'next/link';

export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-background p-24 text-foreground">
      <div className="z-10 w-full max-w-5xl items-center justify-between font-mono text-sm">
        <h1 className="mb-8 text-center text-4xl font-bold tracking-tight">
          {{ project_name }}
        </h1>

        <p className="mb-8 text-center text-lg text-muted-foreground">
          Production-ready SaaS application built with:
        </p>

        <div className="mb-8 grid grid-cols-2 gap-4">
          <div className="rounded-lg border border-border bg-card p-4">
            <h2 className="font-semibold">Runtime</h2>
            <p className="text-sm text-muted-foreground">Next.js 16 + React 19.2</p>
          </div>

          <div className="rounded-lg border border-border bg-card p-4">
            <h2 className="font-semibold">Hosting</h2>
            <p className="text-sm text-muted-foreground">{{ saas_hosting | title }}</p>
          </div>

          <div className="rounded-lg border border-border bg-card p-4">
            <h2 className="font-semibold">Database</h2>
            <p className="text-sm text-muted-foreground">{{ saas_database | title }} + {{ saas_orm | title }}</p>
          </div>

          <div className="rounded-lg border border-border bg-card p-4">
            <h2 className="font-semibold">Authentication</h2>
            <p className="text-sm text-muted-foreground">{{ saas_auth_provider | title }}</p>
          </div>
        </div>

        <div className="flex justify-center gap-4">
          <Link
            href="/dashboard"
            className="rounded-lg bg-primary px-6 py-3 text-primary-foreground transition hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
          >
            Go to Dashboard
          </Link>

          <Link
            href="/api/health"
            className="rounded-lg border border-border bg-card px-6 py-3 transition hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
          >
            Health Check
          </Link>
        </div>

        <div className="mt-12 text-center text-sm text-muted-foreground">
          <p>Generated with Riso SaaS Starter</p>
          <p className="mt-2">
            See <code className="rounded bg-muted px-2 py-1 font-mono">saas-starter.config.ts</code> for your technology stack
          </p>
        </div>
      </div>
    </main>
  );
}
{% endif %}
