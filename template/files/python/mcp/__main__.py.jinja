{%- if mcp_module == 'enabled' and mcp_language == 'python' -%}
"""Entry point for running {{ package_name }}.mcp as a module.

Usage:
    python -m {{ package_name }}.mcp [options]
    uv run python -m {{ package_name }}.mcp [options]
"""

from __future__ import annotations

from .server import main

if __name__ == "__main__":
    main()
{%- endif %}
