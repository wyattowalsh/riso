{%- if mcp_module == 'enabled' and 'python' in mcp_languages -%}
"""{{ package_name }} MCP Server package.

Exposes {{ project_name }} functionality as MCP tools, resources, and prompts.
"""

from .server import __version__, mcp, run_server

__all__ = ["__version__", "mcp", "run_server"]
{%- else -%}
"""MCP module placeholder.

Enable via `mcp_module=enabled` and `mcp_languages=[python]` to generate.
"""
{%- endif %}
