"""API route modules.

This package contains all API route definitions organized by domain.
Each route module should define an APIRouter instance and register
endpoints following FastAPI best practices.
"""

{%- if api_tracks | lower in ["python", "python+node"] %}
from __future__ import annotations

from .health import router as health_router

__all__ = ["health_router"]
{%- else %}
# API routes disabled - enable with api_tracks=python or python+node
{%- endif %}
