{%- if api_tracks | lower not in ["python", "python+node"] %}
"""FastAPI service disabled for this render."""
{%- else %}
"""FastAPI API module.

This package contains the FastAPI application with modular routing,
Pydantic validation, CORS middleware, error handling, and comprehensive testing.

Main components:
- main.py: Application factory and router registration
- config.py: Environment-based configuration
- routes/: API endpoint modules
- models/: Request/response models
- middleware/: CORS and error handling

Example:
    >>> from {{ package_name }}.api.main import app
    >>> # Run with: uvicorn {{ package_name }}.api.main:app --reload
"""

from __future__ import annotations

__all__ = ["main", "config"]
{%- endif %}
