{%- if api_module != 'enabled' or 'python' not in api_languages %}
"""Middleware modules disabled for this render."""
{%- else %}
"""API middleware components.

This package contains middleware for cross-cutting concerns like CORS,
error handling, request IDs, and HTTP rate limiting.
"""

from __future__ import annotations

__all__ = ["cors", "errors", "rate_limit"]
{%- endif %}
