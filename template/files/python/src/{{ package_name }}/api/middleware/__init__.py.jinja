"""Middleware components for request/response processing.

This package contains middleware for CORS, error handling, logging,
and other cross-cutting concerns.
"""

{%- if api_tracks | lower in ["python", "python+node"] %}
from __future__ import annotations

__all__: list[str] = []
{%- else %}
# API middleware disabled - enable with api_tracks=python or python+node
{%- endif %}
