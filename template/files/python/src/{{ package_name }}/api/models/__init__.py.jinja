"""Pydantic models for API requests and responses.

This package contains all Pydantic model definitions used for request
validation and response serialization throughout the API.
"""

{%- if api_tracks | lower in ["python", "python+node"] %}
from __future__ import annotations

from .responses import ErrorResponse, HealthResponse

__all__ = ["ErrorResponse", "HealthResponse"]
{%- else %}
# API models disabled - enable with api_tracks=python or python+node
{%- endif %}
