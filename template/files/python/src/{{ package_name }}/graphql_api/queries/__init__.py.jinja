{% if (graphql_api_module == 'enabled' or 'graphql' in api_features) -%}
"""GraphQL Queries - Query resolvers for read operations."""

from {{ package_name }}.graphql_api.queries.user_queries import UserQueries


__all__ = [
    "UserQueries",
]
{% endif -%}
