{% if codegen_module == 'enabled' %}"""Code generation and scaffolding tools for Riso projects.

This module provides CLI commands and programmatic APIs for:
- Generating new projects from templates
- Adding feature modules to existing projects
- Updating projects when templates evolve
- Customizing templates for team-specific patterns
- Generating code from API specifications
"""

from loguru import logger

__version__ = "0.1.0"
__all__ = ["__version__"]

logger.disable(__name__)  # Disable by default, enable in CLI
{% endif %}