# Python package justfile — delegates quality and hook targets to quality/justfile.quality
# Generated by Riso template

import '../quality/justfile.quality'
{% if docs_module == 'enabled' and docs_framework == 'sphinx-shibuya' %}

# Twin of Makefile `linkcheck`. Official smoke honors task_runner (just dests run this).
linkcheck:
    uv run --group docs sphinx-build -b linkcheck docs dist/docs-linkcheck
{% endif %}