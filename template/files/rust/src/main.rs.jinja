{% if (project_language == 'rust' or 'rust' in cli_languages or 'rust' in api_languages or 'rust' in mcp_languages)
    and not (cli_module == 'enabled' and 'rust' in cli_languages)
    and not (api_module == 'enabled' and 'rust' in api_languages) %}
//! Default binary entry when no dedicated CLI/API bins are configured.
//!
//! Prefer the module-specific binaries (`cli/main.rs`, `api/main.rs`) when those
//! modules are enabled. MCP lives under `mcp/` with its own Cargo package.

use anyhow::Result;
use tracing::info;

#[tokio::main]
async fn main() -> Result<()> {
    tracing_subscriber::fmt::init();
    info!("{{ project_name }} — enable CLI/API/MCP modules for full scaffolds");
    Ok(())
}
{% endif %}
