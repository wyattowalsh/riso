{%- if mcp_module == 'enabled' and 'rust' in mcp_languages -%}
use {{ package_name }}_mcp;

#[tokio::test]
async fn test_library_interface() {
    // Verify that we can access the library modules
    let _version = {{ package_name }}_mcp::VERSION;
    let _server_name = {{ package_name }}_mcp::SERVER_NAME;
    
    assert!(!_version.is_empty());
    assert!(!_server_name.is_empty());
}
{%- endif -%}
