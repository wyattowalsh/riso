{%- if mcp_module == 'enabled' and 'rust' in mcp_languages -%}
//! MCP Resources registration
//!
//! Registers all available resources with the MCP server.

use rmcp::Server;

{%- if mcp_example_tools %}
mod system;
{%- endif %}

/// Register all resources with the MCP server
pub fn register(server: &Server) {
{%- if mcp_example_tools %}
    // system::register(server); // TODO: Verify resource registration API
{%- else %}
    // Add your resource registrations here
    let _ = server;
{%- endif %}
}
{%- endif %}
