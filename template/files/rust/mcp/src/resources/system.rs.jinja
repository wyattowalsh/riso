{%- if mcp_module == 'enabled' and 'rust' in mcp_languages and mcp_example_tools -%}
//! System resources - basic system information access.

use rmcp::{Server}; // Add resource macro if available in future rmcp versions

pub fn register(server: &Server) {
    // Implementation pending rmcp resource macro support or explicit handler pattern integration
    // server.register_resource("system://info", ...);
    let _ = server;
}
{%- endif %}
