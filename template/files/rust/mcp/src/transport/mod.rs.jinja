{%- if mcp_module == 'enabled' and 'rust' in mcp_languages and mcp_transport in ['sse', 'http'] -%}
//! Streamable HTTP transport (covers both `http` and `sse` selections).

pub mod http;

pub use http::serve_streamable_http;
{%- endif -%}
