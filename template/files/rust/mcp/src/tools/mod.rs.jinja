{%- if mcp_module == 'enabled' and 'rust' in mcp_languages -%}
//! MCP tool helpers used by [`crate::server::McpServer`].

{%- if mcp_example_tools %}
pub mod echo;
pub mod http_fetch;
pub mod timestamp;
{%- endif %}
{%- endif %}
