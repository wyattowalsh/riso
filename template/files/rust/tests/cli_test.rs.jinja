{% if 'rust' in cli_languages or 'rust' in api_languages or 'rust' in mcp_languages %}
//! Root integration crate for the Rust package.
//!
//! EXTENSION POINT: CLI behavior is covered by `cli_integration_test.rs` when
//! the CLI module is enabled. Do not add `assert!(true)` or `todo!()`
//! placeholders — dest CI must not go green (or fail) on empty tests.
{% endif %}
