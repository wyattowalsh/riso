{% if api_module == 'enabled' and 'rust' in api_languages %}
//! API integration tests
//!
//! The Actix API is a `[[bin]]` (`api/main.rs`), so this crate cannot import
//! handlers. Real HTTP assertions live in `api/handlers/*.rs` unit tests.
//!
//! EXTENSION POINT: add `src/lib.rs` (or process-spawn `{{ package_name }}-api`)
//! before enabling the tests below. Do not use `assert!(true)` (dest CI green
//! on empty tests) or `todo!()` (dest `cargo test` fails).

#[test]
#[ignore = "EXTENSION POINT: import health handler via a library target"]
fn test_health_endpoint() {}

#[test]
#[ignore = "EXTENSION POINT: import hello handler via a library target"]
fn test_api_hello_endpoint() {}

#[test]
#[ignore = "EXTENSION POINT: assert CORS from CORS_ORIGINS / production fail-closed"]
fn test_cors_headers() {}
{% endif %}
