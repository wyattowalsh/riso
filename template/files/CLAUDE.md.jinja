# CLAUDE.md

> Bridge file for Claude Code. See [AGENTS.md](./AGENTS.md) for full instructions.

This project uses **AGENTS.md** as the single source of truth for AI coding agent instructions.
Claude Code will automatically read this file alongside AGENTS.md.

## Quick Reference

- **Project**: {{ project_name }}
- **Package**: `{{ package_name }}`
- **Layout**: {{ project_layout }}

## Instructions

All project-specific instructions, commands, code style guidelines, and recent changes
are maintained in [AGENTS.md](./AGENTS.md). Please refer to that file for:

- Project overview and structure
- Available commands (build, test, lint)
- Code style requirements
- Active technologies
- Recent changes

## MCP Servers

This project includes MCP server configurations in `.mcp.json`. The following server
categories are enabled:

{% if ai_tools_mcp_thinking | default(true) %}- **Thinking**: Sequential, structured, cascade reasoning{% endif %}
{% if ai_tools_mcp_web | default(true) %}- **Web**: Fetch, Playwright browser automation{% endif %}
{% if ai_tools_mcp_documents | default(true) %}- **Documents**: Docling (PDF/DOCX), Repomix{% endif %}
{% if ai_tools_mcp_utilities | default(true) %}- **Utilities**: Memory persistence{% endif %}
{% if ai_tools_mcp_search | default(false) %}- **Search**: Brave, Tavily (requires API keys in env){% endif %}
{% if mcp_module == 'enabled' %}- **Project**: `{{ project_slug }}` MCP server{% endif %}
