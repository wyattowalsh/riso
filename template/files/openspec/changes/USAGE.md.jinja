# Changes — {{ project_name }}

One folder per proposed change:

```text
openspec/changes/<kebab-change-name>/
├── proposal.md
├── design.md
├── tasks.md
└── specs/<domain>/spec.md
```

Active work stays here. Completed work moves to `archive/` (date-prefixed).
This file keeps `changes/` present after Copier render.
