# {{ project_name }} — project extra

## Purpose

Describe current, agreed behavior for this generated project. Add more domains
as `openspec/specs/<domain>/spec.md`.

## Requirements

### Requirement: Extra is optional

The generated project SHALL treat OpenSpec as an optional extra that is off
unless `openspec_extra` is `enabled`.

#### Scenario: Extra enabled
- **WHEN** the project was rendered with `openspec_extra=enabled`
- **THEN** `openspec/config.yaml` and `openspec/AGENTS.md` are present
