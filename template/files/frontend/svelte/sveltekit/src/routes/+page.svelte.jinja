{% if frontend_framework == 'svelte' and svelte_framework == 'sveltekit' %}
<script lang="ts">
  // import { createQuery } from '@tanstack/svelte-query'
  // import { apiClient } from '$lib/api'

  let count = $state(0)

  // Example API call (uncomment when backend is ready)
  // const healthQuery = createQuery({
  //   queryKey: ['health'],
  //   queryFn: () => apiClient.get('/api/health'),
  // })
</script>

<svelte:head>
  <title>{{ "{{ project_name }}" }}</title>
  <meta name="description" content="{{ project_name }}" />
</svelte:head>

<div class="min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 text-white">
  <div class="container mx-auto px-4 py-16">
    <div class="max-w-2xl mx-auto text-center">
      <h1 class="text-5xl font-bold mb-6 bg-gradient-to-r from-orange-400 to-pink-500 bg-clip-text text-transparent">
        {{ "{{ project_name }}" }}
      </h1>
      <p class="text-xl text-slate-300 mb-8">
        SvelteKit 5 + TypeScript + Vite + TailwindCSS
      </p>

      <div class="bg-slate-800/50 backdrop-blur rounded-lg p-8 shadow-xl border border-slate-700">
        <div class="mb-6">
          <button
            onclick={() => count++}
            class="px-6 py-3 bg-orange-600 hover:bg-orange-700 rounded-lg font-semibold transition-colors shadow-lg hover:shadow-orange-500/50"
          >
            Count is {count}
          </button>
        </div>

        <p class="text-slate-400 text-sm">
          Edit <code class="bg-slate-900 px-2 py-1 rounded text-orange-400">src/routes/+page.svelte</code> and save to test HMR
        </p>
      </div>

      <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
        <a
          href="https://svelte.dev"
          target="_blank"
          rel="noopener noreferrer"
          class="p-4 bg-slate-800/30 hover:bg-slate-800/50 rounded-lg border border-slate-700 transition-colors"
        >
          <h3 class="font-semibold text-orange-400 mb-2">Svelte Docs</h3>
          <p class="text-slate-400">Learn Svelte</p>
        </a>
        <a
          href="https://kit.svelte.dev"
          target="_blank"
          rel="noopener noreferrer"
          class="p-4 bg-slate-800/30 hover:bg-slate-800/50 rounded-lg border border-slate-700 transition-colors"
        >
          <h3 class="font-semibold text-pink-400 mb-2">SvelteKit Docs</h3>
          <p class="text-slate-400">Explore SvelteKit</p>
        </a>
        <a
          href="https://tailwindcss.com"
          target="_blank"
          rel="noopener noreferrer"
          class="p-4 bg-slate-800/30 hover:bg-slate-800/50 rounded-lg border border-slate-700 transition-colors"
        >
          <h3 class="font-semibold text-cyan-400 mb-2">Tailwind CSS</h3>
          <p class="text-slate-400">Style Guide</p>
        </a>
      </div>
    </div>
  </div>
</div>
{% endif %}
