{% if frontend_framework == 'svelte' and svelte_framework == 'sveltekit' %}
import { sveltekit } from '@sveltejs/kit/vite'
import { defineConfig } from 'vite'

export default defineConfig({
  plugins: [sveltekit()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: {% if 'python' in backend_languages and python_api_framework == 'fastapi' %}'http://localhost:8000'{% elif 'node' in backend_languages and node_api_framework == 'fastify' %}'http://localhost:3001'{% else %}'http://localhost:8000'{% endif %},
        changeOrigin: true,
      },
    },
  },
})
{% endif %}
