{% if frontend_framework == 'vue' and vue_framework == 'vite' %}
<script setup lang="ts">
import { ref } from 'vue'
// import { useQuery } from '@tanstack/vue-query'
// import { apiClient } from './lib/api'

const count = ref(0)

// Example API call (uncomment when backend is ready)
// const { data, isLoading, error } = useQuery({
//   queryKey: ['health'],
//   queryFn: () => apiClient.get('/api/health'),
// })
</script>

<template>
  <div class="min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 text-white">
    <div class="container mx-auto px-4 py-16">
      <div class="max-w-2xl mx-auto text-center">
        <h1 class="text-5xl font-bold mb-6 bg-gradient-to-r from-emerald-400 to-cyan-500 bg-clip-text text-transparent">
          {{ "{{ project_name }}" }}
        </h1>
        <p class="text-xl text-slate-300 mb-8">
          Vue 3 + TypeScript + Vite + TailwindCSS
        </p>

        <div class="bg-slate-800/50 backdrop-blur rounded-lg p-8 shadow-xl border border-slate-700">
          <div class="mb-6">
            <button
              @click="count++"
              class="px-6 py-3 bg-emerald-600 hover:bg-emerald-700 rounded-lg font-semibold transition-colors shadow-lg hover:shadow-emerald-500/50"
            >
              Count is {{ "{{ count }}" }}
            </button>
          </div>

          <p class="text-slate-400 text-sm">
            Edit <code class="bg-slate-900 px-2 py-1 rounded text-emerald-400">src/App.vue</code> and save to test HMR
          </p>
        </div>

        <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
          <a
            href="https://vuejs.org"
            target="_blank"
            rel="noopener noreferrer"
            class="p-4 bg-slate-800/30 hover:bg-slate-800/50 rounded-lg border border-slate-700 transition-colors"
          >
            <h3 class="font-semibold text-emerald-400 mb-2">Vue Docs</h3>
            <p class="text-slate-400">Learn Vue</p>
          </a>
          <a
            href="https://vitejs.dev"
            target="_blank"
            rel="noopener noreferrer"
            class="p-4 bg-slate-800/30 hover:bg-slate-800/50 rounded-lg border border-slate-700 transition-colors"
          >
            <h3 class="font-semibold text-purple-400 mb-2">Vite Docs</h3>
            <p class="text-slate-400">Explore Vite</p>
          </a>
          <a
            href="https://tailwindcss.com"
            target="_blank"
            rel="noopener noreferrer"
            class="p-4 bg-slate-800/30 hover:bg-slate-800/50 rounded-lg border border-slate-700 transition-colors"
          >
            <h3 class="font-semibold text-cyan-400 mb-2">Tailwind CSS</h3>
            <p class="text-slate-400">Style Guide</p>
          </a>
        </div>
      </div>
    </div>
  </div>
</template>
{% endif %}
