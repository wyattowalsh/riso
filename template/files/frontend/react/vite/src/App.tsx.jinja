{% if frontend_framework == 'react' and react_framework == 'vite' %}
import { useState } from 'react'
import { useQuery } from '@tanstack/react-query'
import { apiClient } from './lib/api'

function App() {
  const [count, setCount] = useState(0)

  // Example API call (uncomment when backend is ready)
  // const { data, isLoading, error } = useQuery({
  //   queryKey: ['health'],
  //   queryFn: () => apiClient.get('/api/health'),
  // })

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 text-white">
      <div className="container mx-auto px-4 py-16">
        <div className="max-w-2xl mx-auto text-center">
          <h1 className="text-5xl font-bold mb-6 bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent">
            {{ project_name }}
          </h1>
          <p className="text-xl text-slate-300 mb-8">
            React + TypeScript + Vite + TailwindCSS
          </p>

          <div className="bg-slate-800/50 backdrop-blur rounded-lg p-8 shadow-xl border border-slate-700">
            <div className="mb-6">
              <button
                onClick={() => setCount((count) => count + 1)}
                className="px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-lg font-semibold transition-colors shadow-lg hover:shadow-blue-500/50"
              >
                Count is {count}
              </button>
            </div>

            <p className="text-slate-400 text-sm">
              Edit <code className="bg-slate-900 px-2 py-1 rounded text-blue-400">src/App.tsx</code> and save to test HMR
            </p>
          </div>

          <div className="mt-12 grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
            <a
              href="https://react.dev"
              target="_blank"
              rel="noopener noreferrer"
              className="p-4 bg-slate-800/30 hover:bg-slate-800/50 rounded-lg border border-slate-700 transition-colors"
            >
              <h3 className="font-semibold text-blue-400 mb-2">React Docs</h3>
              <p className="text-slate-400">Learn React</p>
            </a>
            <a
              href="https://vitejs.dev"
              target="_blank"
              rel="noopener noreferrer"
              className="p-4 bg-slate-800/30 hover:bg-slate-800/50 rounded-lg border border-slate-700 transition-colors"
            >
              <h3 className="font-semibold text-purple-400 mb-2">Vite Docs</h3>
              <p className="text-slate-400">Explore Vite</p>
            </a>
            <a
              href="https://tailwindcss.com"
              target="_blank"
              rel="noopener noreferrer"
              className="p-4 bg-slate-800/30 hover:bg-slate-800/50 rounded-lg border border-slate-700 transition-colors"
            >
              <h3 className="font-semibold text-cyan-400 mb-2">Tailwind CSS</h3>
              <p className="text-slate-400">Style Guide</p>
            </a>
          </div>
        </div>
      </div>
    </div>
  )
}

export default App
{% endif %}
