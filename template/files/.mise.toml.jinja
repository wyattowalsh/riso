# Generated by the Riso template; managed via template/files/.mise.toml.jinja

[tools]
node = "20"
pnpm = "9.15.0"
uv = "0.4.30"
{%- if 'rust' in mcp_languages or 'rust' in cli_languages or 'rust' in api_languages %}
rust = "1.81"
{%- endif %}
{%- if task_runner in ['just', 'both'] %}
just = "1.34"
{%- endif %}

[settings]
auto_install = true