{#- Shared quick-reference rows for AGENTS.md — keep in sync with post_gen + tasks/quality.py -#}
{%- macro python_surface_enabled() -%}
{{ (cli_module == 'enabled' and 'python' in cli_languages) or (api_module == 'enabled' and 'python' in api_languages) or (mcp_module == 'enabled' and 'python' in mcp_languages) or (docs_module == 'enabled' and docs_framework == 'sphinx-shibuya') }}
{%- endmacro -%}

{%- macro node_surface_enabled() -%}
{{ (api_module == 'enabled' and 'node' in api_languages) or (mcp_module == 'enabled' and 'typescript' in mcp_languages) or (docs_module == 'enabled' and docs_framework in ['fumadocs', 'docusaurus']) or saas_infra_module == 'enabled' or desktop_module == 'enabled' or changelog_module == 'enabled' }}
{%- endmacro -%}
