# Cursor — Riso Maintainer Repo

Read [AGENTS.md](../AGENTS.md) first. This file is a pointer only.

- Maintainer repo: edit `template/`, hooks, `scripts/ci/` — not `samples/*/render/`
- Commands: `make help` · `make quality` · always `uv run` for Python
- Spec Kit: `.cursor/commands/speckit.*` · specs in `specs/`
- Skills: `.agents/skills/riso-scaffold/` for template operations