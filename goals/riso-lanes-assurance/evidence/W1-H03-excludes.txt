14:    Rust CLI + Python API + Fumadocs. Features modular SaaS scaffolding with
1981:  # Rust CLI - exclude if not using Rust CLI
1982:  - "{% if not (cli_module == 'enabled' and 'rust' in cli_languages) %}rust/cli/{% endif %}"
1984:  # Rust API - exclude if not using Rust API
1985:  - "{% if not (api_module == 'enabled' and 'rust' in api_languages) %}rust/api/{% endif %}"
