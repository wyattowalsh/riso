# PLATFORM audit — 2026-07-25

## Executive summary

First PLATFORM `/goal` run completed standing ops bootstrap, sample-answer currency fixes, quality/testing review, CI coverage pack tests, render-path hardening for local mise/hook imports, and full-matrix attempt (answers changed).

## Ops artifacts

| Artifact | Status |
|----------|--------|
| `inbox/` + `_TEMPLATE.md` | present |
| `outbox/` + `_TEMPLATE.md` | present |
| `OPERATING.md` | present |
| `audit/*` | present |

## Answers audit

- Validated all 23 `samples/*/copier-answers.yml`.
- **PLATFORM fixes:** normalized `api_features` strings → multiselect lists (12 variants).
- **Post-fix:** 22/23 validate OK.
- **COORD handoff:** `outbox/coord-mcp-languages-typescript.md` (`mcp_languages: typescript` not in contract choices).
- **MATRIX_REQUIRED:** true (see `matrix-decision.md`).

Evidence: `audit/validate/*.json`, scratch `validate-*.json`.

## Quality / testing

- `check_quality_parity.py` → pass (scratch `quality-parity.log`).
- Quality + testing reviews written; no payload edits required this run.
- Cross-lane: PY owns `template/files/python/tasks/quality.py.jinja` (parity currently green).

## CI coverage pack

| Module | Test file | Status |
|--------|-----------|--------|
| `run_quality_suite.py` | `tests/unit/ci/test_run_quality_suite.py` | added |
| `generate_matrix_data.py` | `tests/unit/ci/test_generate_matrix_data.py` | added |
| `verify_version_sync.py` | `tests/unit/ci/test_verify_version_sync.py` | added |
| deferred modules | `audit/COVERAGE_GAPS.md` | documented |

Also fixed PLATFORM render reliability:

| Change | Why |
|--------|-----|
| `scripts/hooks/workflow_validator.py` logger import | post_gen disabled tooling when only `scripts/` on path |
| `scripts/hooks/quality_tool_check.py` `MISE_TRUSTED_CONFIG_PATHS` | untrusted generated `.mise.toml` broke `uv tool run` |
| `scripts/render-samples.sh` `trust_render_mise_config` + smoke timeout | local mise + hung smokes blocked matrix |

Unit evidence: `tests/unit/ci/test_workflow_validator_import.py`, `test_quality_tool_check.py`.

## Matrix

- Full `uv run python scripts/ci/render_matrix.py` required after answer changes.
- Default variant smoke proved green after hardening (`smoke-results.json`).
- Full matrix log: scratch `render_matrix.log` (see completion status in session).

## Facts.md mapping

| Fact theme | Evidence / deferral |
|------------|---------------------|
| Lane charter / exclusive roots | `OPERATING.md` |
| Forbidden writes | git scope: no edits under language trees/copier/hooks/src/web from PLATFORM work (pre-existing dirty go/ files not PLATFORM) |
| Inbound triggers | `OPERATING.md` + inbox templates |
| Investigate any CI red, no foreign edits | outbox COORD for mcp-typescript; no foreign tree patches |
| Sample answers all-affected | 12 list fixes; scan all 23 |
| Validate touched answers | 22 green; 1 COORD-blocked |
| Auto full matrix after answers | matrix run initiated/logged |
| No hand-edit renders | only via `render-samples.sh` / `render_matrix.py` |
| Payload bug outbox | `outbox/coord-mcp-languages-typescript.md` |
| quality/testing consistency | parity pass + reviews |
| CI unit tests on scripts change | pack + import/mise tests |
| just quality when broad | scripts/hooks + render-samples changed — run if time; pack pytest green |
| Conditional context/agents | not required this change set |
| Workflow glue minimal | no workflow YAML edits |
| Bootstrap artifacts | ops files present |
| First-run audit | this document |
| Known backlog answer currency | done for api_features |
| Known backlog CI coverage | pack done + deferred listed |
| Known backlog quality payload | reviewed, no edits |
| Git hygiene | no branch/commit/push |

## Outbox

1. `coord-mcp-languages-typescript.md` → COORD

## Git scope notes

PLATFORM intentional writes:

- `samples/*/copier-answers.yml` (12)
- `scripts/hooks/workflow_validator.py`, `quality_tool_check.py`
- `scripts/render-samples.sh`
- `tests/unit/ci/test_*.py` (new)
- `goals/riso-lane-platform/**`

Pre-existing non-PLATFORM dirty paths observed (do not treat as PLATFORM): `template/files/go/**`, `.claude/skills/mcp-installer/`.
