# W4-D03 — CHANGELOG Unreleased 2.0.0 breaking remaps

- Task: `W4-D03`
- Wave: W4
- Lane: DOCS
- Repo: `/Users/ww/dev/projects/riso`
- Branch: `main` (`f7951fe62e7c635f3a90d17811d3711c2a2d7c1b`) — no checkout / stash / reset / rebase
- Exclusive writes: `CHANGELOG.md`, this evidence file
- `samples/*/render/**` writes: **0**
- Git tag created: **none** (`git tag -l 'v2.0.0' '2.0.0'` empty)
- Status: **green**
- Date (UTC): 2026-08-14

## Contract

`plan.md` verify: CHANGELOG **Unreleased 2.0.0** section exists, names breaking remaps, **no tag**.

Remap SSOT (read-only): `src/riso/core/removed_answer_keys.py` `ANSWER_KEY_REMAPS` + `REMOVED_ANSWER_KEYS` (8/8). Apply then reject; no dest overwrite; idempotent; leftovers fail closed.

## Section

Heading: `## [Unreleased] 2.0.0` inserted after the Keep a Changelog preamble and before `[1.2.10]`.

| Check | Result |
| --- | --- |
| heading `## [Unreleased] 2.0.0` | present (1) |
| `### ⚠ BREAKING CHANGES` | present |
| all 8 old keys named | `api_tracks`, `api_language`, `docs_site`, `mcp_language`, `saas_starter_module`, `saas_auth`, `saas_billing`, `include_admin` |
| operators named | derive / wrap-list / rename / split / rename-bool |
| dest keys named | `api_module`, `api_languages`, `docs_module`, `docs_framework`, `mcp_languages`, `saas_infra_module`, `saas_auth_module`, `saas_auth_provider`, `saas_billing_module`, `saas_billing_provider`, `saas_admin_dashboard` |
| apply-then-reject | `apply_removed_key_remaps` then reject leftovers |
| no dest overwrite | stated |
| idempotent | stated |
| no dual-path after remap | stated |
| `riso migrate DEST|--answers-file PATH [--dry-run] [--json]` | stated |
| `riso update` remaps before Copier | stated |
| no `v2.0.0` tag sentence | stated in section |
| `v2.0.0` / `2.0.0` tags | **absent** |

A leftover historical `## [Unreleased]` remains at the bottom of `CHANGELOG.md` (pre-1.0 notes). That is not the 2.0.0 section; it was not rewritten.

## Verify

```text
python3 -c 'from pathlib import Path; t=Path("CHANGELOG.md").read_text(); assert "## [Unreleased] 2.0.0" in t'
git tag -l 'v2.0.0' '2.0.0'
git status --short -- 'samples/*/render/**'
```

- heading assertion: **ok**
- tags: **empty**
- render/ dirty paths: **0**

## Dep note (not this slice)

`W4-D03` lists `W4-D01` as a dep. `docs/guides/v2-migration.md` was **not** on disk at write time. The CHANGELOG points at that planned path. D01 remains a DOCS owner; D03 verify (section exists, remaps named, no tag) is met from the remap SSOT.

## Not this slice

- No commit / tag / push / PyPI.
- `BREAKING_CHANGES.md` and `docs/changelog.md` are outside the exclusive write.
- `docs/guides/v2-migration.md` is W4-D01.
