# W4-D04 + W4-D05 — generated upgrade-guide + AGENTS pointers

- Tasks: `W4-D04`, `W4-D05`
- Wave: W4
- Lane: DOCS
- Date: 2026-08-13 (UTC command: 2026-08-14T00:14:30Z)
- Repo: `/Users/ww/dev/projects/riso`
- Branch: `main` @ `f7951fe` (no checkout / stash / reset / commit / tag / push)
- Exclusive writes: `template/files/docs/upgrade-guide.md.jinja`, `template/files/AGENTS.md.jinja`, this file
- `samples/*/render/**` writes: **0**
- Status: **green**

## Filter / command

```text
git rev-parse --show-toplevel   # /Users/ww/dev/projects/riso
git rev-parse --abbrev-ref HEAD # main

uv run python scripts/ci/validate_jinja_templates.py \
  template/files/docs/upgrade-guide.md.jinja \
  template/files/AGENTS.md.jinja
# Validated 2 Jinja template(s): all OK

uv run pytest tests/unit/template/test_agents_md_render.py -q -n 0
# 10 passed in 0.18s
```

Lockstep source (read-only): `docs/guides/v2-migration.md` (W4-D01, sibling; not edited),
`template/prompts/v2.yml.jinja`, `src/riso/core/removed_answer_keys.py`,
`goals/riso-v2-release-ready/plan.md` remap table, W1 outbox `remap-ssot` /
`mise-always` / `openspec-extra`.

## W4-D04 — `template/files/docs/upgrade-guide.md.jinja`

Generated-project upgrade guide now locksteps 2.0 remaps, mise, and OpenSpec extra.

| Surface | Present |
| --- | --- |
| `apply_removed_key_remaps` then reject leftovers | yes |
| 8 remaps (`api_tracks`, `api_language`, `docs_site`, `mcp_language`, `saas_starter_module`, `saas_auth`, `saas_billing`, `include_admin`) | yes; same operators/dests as D01 / SSOT |
| `riso migrate DEST\|--answers-file [--dry-run] [--json]` | yes; `--json` noted as global |
| `riso update` remaps before Copier | yes |
| Do-not-overwrite dest; idempotent; no dual-path | yes |
| Unmapped leftovers fail closed (`firebase` / `mkdocs`) | yes |
| `graphql_api_module` / `websocket_module` not remapped | yes |
| mise always-on (`mise.toml`, `mise install`, Python 3.11 / Node **20** / pnpm / uv) | yes; Node not raised to maintainer 22 |
| `openspec_extra` default **disabled**; does not gate mise | yes |
| Quality: **ty** (not mypy), **just**, **pnpm** | yes |
| Pointers to `DESIGN.md` + `AGENTS.md` | yes |

Removed maintainer-only `scripts/ci/render_matrix.py` / `verify_context_sync.py`
blocks (those do not belong in a generated project).

`git diff --stat -- template/files/docs/upgrade-guide.md.jinja`:
`169` lines in the combined exclusive-pair diff (upgrade-guide is the bulk).

## W4-D05 — `template/files/AGENTS.md.jinja`

Pointers only (no second remap table). DESIGN.md is referenced, not edited
(not an exclusive write root; D05 lock is AGENTS).

| Pointer | Location |
| --- | --- |
| mise always-on + `mise install` + Node 20 | Overview, Quick Reference row, Setup, Active Technologies, Recent Changes |
| `openspec_extra` default disabled | Overview, Module Guides (conditional `openspec/USAGE.md`), Active Technologies, Recent Changes |
| **ty** / **just** / **pnpm** | Overview Quality defaults, Quick Reference, Active Technologies, Recent Changes, DESIGN Tooling pointer |
| Remap contract + `riso migrate --dry-run` | Setup + Never Touch (8 keys) + Recent Changes → `docs/upgrade-guide.md` |
| DESIGN.md Tooling table (just / ty / pnpm) | Overview + References |

`openspec_extra | default('disabled')` so existing
`tests/unit/template/test_agents_md_render.py` StrictUndefined context still
renders (10/10 passed). Quick-reference rows stay unmerged (`| ------- ||` absent).

## Verify

```text
uv run python scripts/ci/validate_jinja_templates.py \
  template/files/docs/upgrade-guide.md.jinja template/files/AGENTS.md.jinja
# Validated 2 Jinja template(s): all OK

uv run pytest tests/unit/template/test_agents_md_render.py -q -n 0
# ============================== 10 passed in 0.18s ==============================

# StrictUndefined render smoke (upgrade-guide + AGENTS, with and without openspec_extra)
# OK render AGENTS.md.jinja
# OK render docs/upgrade-guide.md.jinja
# OK AGENTS without openspec_extra key (default filter)

git status --short -- 'samples/*/render'
# (empty)  → write count 0
```

`uv run riso migrate` is registered (`Remap removed Copier answer keys (1.x → 2.0).`).

## Keep / drop vs plan.md

| Item | Decision |
| --- | --- |
| Generated upgrade-guide remaps + mise + OpenSpec extra | KEEP / done (W4-D04) |
| AGENTS pointers mise / OpenSpec extra / ty / just / pnpm | KEEP / done (W4-D05) |
| SaaS flatten | not this lane |
| `docs/guides/v2-migration.md` | sibling W4-D01 (read for lockstep; not edited) |
| `CHANGELOG.md` Unreleased 2.0.0 | sibling W4-D03 (not edited) |
| Maintainer `docs/upgrade-guide.md.jinja` | not this exclusive write (W0 listed it; plan lock is the generated jinja) |
| `template/files/DESIGN.md.jinja` | already has ty/just/pnpm Tooling table; mise/OpenSpec live in AGENTS. Not edited. |
| `template/files/CLAUDE.md.jinja` | bridge pointer; not this exclusive write |
| `samples/*/render/**` | never hand-edited |

## Not this lane

- `docs/guides/v2-migration.md`, `docs/guides/index.md`, `CHANGELOG.md` (D01–D03)
- `docs/upgrade-guide.md.jinja` (maintainer Sphinx page)
- `template/files/DESIGN.md.jinja`, `template/files/CLAUDE.md.jinja`
- `template/copier.yml` / hooks / remap SSOT / samples answers
- `sphinx-build -W` (PL-T08)

## Residual

None. D04/D05 verify (`mentions remaps`, `jinja valid`) green.
