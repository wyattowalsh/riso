# W6-NODE-tests

- **Wave:** W6 / NODE residual R2
- **Task:** `CLOSE-NODE / test-node-templates`
- **Lane:** GOAL (exclusive write `tests/unit/test_node_templates.py` + this file)
- **Date (UTC):** 2026-08-18T07:34:00Z
- **Repo:** `/Users/ww/dev/projects/riso`
- **Branch:** `main`
- **HEAD:** `ddc50a0081ed6a5159cd308594d5bfaad7c9971f` (worktree dirty; this session did not commit)
- **`template/files/node/**` writes:** 0 (NODE lock; tests assert current contracts only)
- **`samples/*/render/**` writes:** 0
- **Lockfile / secrets / tag / push:** 0
- **Status:** **green** — 18 passed

## Residual

`goals/riso-v2-release-ready/residuals/NODE.md` R2 — maintainer pytest for NODE close contracts (`tests/**` is foreign to the NODE lock).

## Added

`tests/unit/test_node_templates.py` — source + light jinja-render assertions for live R2 contracts. Style matches `tests/unit/test_electron_templates.py` / `tests/unit/test_go_templates.py` (file-local fixtures, class groups, `pytest.importorskip("jinja2")`).

| Contract                                                   | Assertion                                                                                                                                                |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| no `export { mermaidThemeByColorMode }`                    | `docusaurus.config.ts.jinja` keeps `const mermaidThemeByColorMode`; named export absent in source, rendered config, and all `docs/docusaurus/**/*.jinja` |
| no `require('./docs/api-reference/sidebar.js')`            | absent in `sidebars.ts.jinja`, rendered sidebars, and all `docs/docusaurus/**/*.jinja`; autogenerated `dirName: 'api-reference'` remains                 |
| no `template/files/node/docs/fumadocs/middleware.ts.jinja` | file must not exist                                                                                                                                      |
| fumadocs `output: 'export' as const`                       | present in `next.config.ts.jinja` source and render                                                                                                      |
| sitemap / robots static flags                              | `export const dynamic = 'force-static'` and `export const revalidate = false` in both jinja files (source + render)                                      |
| api-node package                                           | `package.json.jinja` exists; rendered JSON `name=api-node`, `engines.node` major `>=20`, `fastify` + `vitest` present                                    |
| `docs/docusaurus/tailwind.config.ts.jinja` stays deleted   | file must not exist                                                                                                                                      |

## Verify

```text
uv run pytest tests/unit/test_node_templates.py -q -n 0
# 18 passed in 0.31s

uv run ruff check tests/unit/test_node_templates.py
# All checks passed!

uv run ruff format --check tests/unit/test_node_templates.py
# 1 file already formatted
```

**Pass count: 18 passed / 0 failed / 0 skipped.**

## Path lock

| Class                     | Count                               |
| ------------------------- | ----------------------------------- |
| Product / template writes | 0                                   |
| Test write                | `tests/unit/test_node_templates.py` |
| Evidence                  | this file                           |
| `residuals/**`            | 0 (not this exclusive write)        |
| `samples/*/render/**`     | 0                                   |

## Not this lane

- Official dest re-render (`residuals/NODE.md` R1) — PLATFORM
- `npm_surfaces.json` api-node row (R3) — PLATFORM
- Editing `template/files/node/**` — NODE lock
